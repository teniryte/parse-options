{
  "name": "kit-parse-options",
  "version": "2.0.1",
  "homepage": "https://github.com/teniryte/parse-options",
  "description": "CLI arguments parser",
  "keywords": [
    "cli",
    "argv",
    "arguments",
    "args",
    "argv",
    "options",
    "parse"
  ],
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon -q --watch 'src/**/*' --watch 'src/**/*' --ext 'ts,json' --exec 'ts-node src/app.ts'",
    "test": "nodemon -q --watch 'test/**/*' --watch 'src/**/*' --ext 'ts,json' --exec 'mocha -w -r ts-node/register test/index.test.ts'"
  },
  "license": "MIT",
  "author": {
    "name": "teniryte",
    "email": "teniryte@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:teniryte/parse-options.git"
  },
  "publish": true,
  "devDependencies": {
    "@types/chai": "^4.3.5",
    "@types/mocha": "^10.0.1",
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "dependencies": {
    "kit-parse-options": "^2.0.0"
  }
}
